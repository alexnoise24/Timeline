PORT=5000
MONGODB_URI=mongodb://localhost:27017/wedding-timeline
JWT_SECRET=tu_secreto_jwt_super_seguro_aqui_cambiar_en_produccion
NODE_ENV=development

# Firebase Admin SDK (for push notifications)
# In development: uses config/firebase-service-account.json file
# In production: set this to the entire JSON content as a single-line string
FIREBASE_SERVICE_ACCOUNT=
